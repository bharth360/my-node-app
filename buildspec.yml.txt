version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 16
    commands:
      - npm install
  build:
    commands:
      - npm test
      - zip -r app.zip . -x "*.git*" "node_modules/*"
artifacts:
  files:
    - app.zip
